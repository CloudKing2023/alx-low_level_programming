#include "main.h"

/**
 * _isdigit - check for uppercase characters
 * @c: character to be checked
 * Return: 1 for success , 0 for anything else.
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
